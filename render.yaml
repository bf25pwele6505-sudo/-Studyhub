services:
  - type: web
    name: studyhub
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: STUDYHUB_DATA_DIR
        value: /tmp/studyhub
